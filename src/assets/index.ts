import { colors } from './colors/colors';
import { images } from './images/images';
import { strings } from './strings';

export const assets = {
    images: images,
    colors: colors,
    strings: strings
}